const cliente = {
    nome: "Diego",
    idade: 22,
    sexo: "M",
    estado: "DF"
};

console.log(`Meu nome é ${cliente.nome}, tenho ${cliente.idade} anos. Sou do sexo ${cliente.sexo} e moro no estado ${cliente.estado}.`);